# AI layer

Provider code and assistant behavior live here.

This separation is intentional so future versions can add streaming,
memory, model routing, tools, prompt versions, and additional providers
without tightly coupling those features to the chat UI.
